/*
Write a recursive function that takes 
a string pointer and counts the vowels, 
but with a twist: if a vowel is immediately 
followed by a consonant, that vowel counts twice.

Sample Input: "abecid"

Sample Output: 5 
(Explanation: 'a' is followed by 'b' (counts as 2), 
'e' is followed by 'c' (counts as 2), 'i' is followed 
by 'd' (counts as 1). Total = 5)
*/