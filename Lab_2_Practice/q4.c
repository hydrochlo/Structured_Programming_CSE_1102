/*
Write a recursive function int countNestedBrackets(char *str, int *maxDepth) 
that counts the total number of characters 
hidden inside pairs of parentheses (). Use 
the pointer maxDepth to track the deepest 
level of nested parentheses encountered.

Sample Input: "abc(de(f)gh)i"

Sample Output: 6 (Characters inside are d, e, f, g, h, 
and the inner parenthesis count doesn't add to 
characters but depth becomes 2)
*/