#include <stdio.h>

int main(){ 
    int a, b, c, d, e;
    float f;

    char ch1[50], ch2[50];

    a = scanf("%d%s%d%f%d", &b, ch1, &d, &f, ch2);

    if(a > printf("test")){
        printf("school");
    } else {
        printf("discipline");
    }

    return 0;
}